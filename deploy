#!/bin/bash
# Deploy the Python app to shinyapps.io

# First:
# Archive existing app
# Delete existing app
# Then run this script

cd binomialbias
rm -rf rsconnect-python
rsconnect deploy shiny . --name sciris --title "binomialbias"
